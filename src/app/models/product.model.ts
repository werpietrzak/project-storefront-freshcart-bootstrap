export interface ProductModel {
  name: string;
  price: number;
  categoryId: string;
  ratingValue: number;
  ratingCount: number;
  imageUrl: string;
  featureValue: number;
  storeIds: string[];
  id: string;
}
